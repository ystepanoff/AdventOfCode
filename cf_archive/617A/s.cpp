#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int x;
    while (cin >> x) {
        cout << (x + 4) / 5 << '\n';
    }
    return 0;
}
